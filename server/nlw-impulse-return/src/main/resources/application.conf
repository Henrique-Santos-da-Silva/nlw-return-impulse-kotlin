ktor {
    deployment {
        port = 8080
    }
    application {
        modules = [ server.impulse.com.nextlevelweek.ApplicationKt.module ]
    }
}
storage {
    # SQLite
#     sqliteDriver="org.sqlite.JDBC"
#     jdbcUrl = "jdbc:sqlite:"
#     dbFilePath = build/db/localdatabase

    # Postgres
    postgresDriver = "org.postgresql.Driver"
    jdbcUrl = ${?JDBC_URL}
}
